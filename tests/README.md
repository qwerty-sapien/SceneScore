# Test scope

Shared fixtures run independently through Python jsonschema and TypeScript Ajv plus explicit semantic validators. Generated TypeScript comes from the canonical schema. Fake clock, approval hash, registry path/cache/budget, stream-gap and in-process service tests are software evidence only.

NOT RUN / future domain evidence: genuine Muse recording/accuracy, human calibration labels, rendered geometry, non-silent synthesized music, human audition, browser audio unlock/output latency, live API access and full replay performance. No passing placeholder tests represent those tracks.
