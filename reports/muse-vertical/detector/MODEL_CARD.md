# Detector ladder — PIPELINE_TESTED_ONLY

No real model was trained and no real held-out evaluation ran. The inspected
project runtime cannot import `pylsl`; the transport diagnostic read zero samples
and reported `OPTIONAL_DEPENDENCY_UNAVAILABLE`. Device generation, firmware,
verified rate, channel order/units and contact quality remain unverified.

Rung A remains the attributed RAGTM candidate port; it has no control authority.
Rung B is the existing deterministic causal median/MAD detector and double-only
closure grammar. Rung C remains the bounded regularized logistic research
primitive over six causal features. Fixture fitting does not establish blink
performance. No self-training, model selection, online adaptation or deployment
promotion occurred.

`readiness.json` records exact implementation/default configuration hashes, null
real metrics, the unclaimed matcher and all blockers. `synthetic-causal-replay.json`
contains a deterministic two-pass raw-fixture replay with one accepted double.
That file tests software behavior and is not evidence that a person blinked.
`pytest.txt` is the scoped test output; warning details remain visible.

The collector preserves consent/refit/split provenance before samples, offers
instructed and natural activity protocols, and requires delayed independent
review. It records each source clock segment separately. A frozen session/refit
index rejects leakage and binds consent bytes. Final-test matcher settings must
be preregistered on development data before claiming `FinalTestLock`. No final
set exists in this run and no lock was created.

The next status requires consented, independently labelled verified Muse sessions
across separate refits with a frozen split/matcher, followed by causal real
replay evaluation. `SUPERVISED_DEMO_READY` additionally requires the existing
pilot, integration and audition gates; software tests do not satisfy them.

Rollback to the deterministic baseline:

```sh
PYTHONPATH=.:src .venv/bin/python -m modules.muse.acquisition replay SESSION --detect --arm-after-s 1.0
```

This command uses default Config and never silently rearms after a fault. Grammar
uses end-to-next-onset gaps and closure strictly after end plus 0.5 seconds. The
pack's onset-gap wording differs; defaults were preserved. Physical, device-to-
host and real detector latency remain null. Scheduled audio timing belongs to
the separate integration report.
