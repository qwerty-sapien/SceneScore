diff --git a/apps/training/src/api.ts b/apps/training/src/api.ts
index 2389e01..8eb1d06 100644
--- a/apps/training/src/api.ts
+++ b/apps/training/src/api.ts
@@ -1,4 +1,4 @@
-import type {MarkerRequest,ModelSummary,Review,ReviewBundle,Role,Source,Status,Trace} from './types';
+import type {DiagnosticsReport,MarkerRequest,ModelSummary,Review,ReviewBundle,Role,Source,Status,Trace} from './types';
 export const API_ROOT='http://127.0.0.1:8767/v1';
 export class TrainingAPI {
  private session:string|null=null;
@@ -14,6 +14,9 @@ export class TrainingAPI {
  private async json<T>(path:string,body?:unknown,signal?:AbortSignal):Promise<T>{return (await this.response(path,body,undefined,signal)).json();}
  async authenticate(token:string):Promise<Status>{const trimmed=token.trim();if(trimmed.length<16||trimmed.length>256||/\s/.test(trimmed))throw Error('Paste the token printed by the local training service.');const result=await(await this.response('/session',{},trimmed)).json();if(typeof result.session!=='string')throw Error('Invalid local session response.');this.session=result.session;return result.status;}
  status(signal?:AbortSignal){return this.json<Status>('/status',undefined,signal);}
+ diagnostics(signal?:AbortSignal){return this.json<DiagnosticsReport>('/diagnostics',undefined,signal);}
+ monitorDiagnostics(enabled:boolean){return this.json<DiagnosticsReport>('/diagnostics/monitor',{enabled});}
+ checkDiagnostics(request_bluetooth_permission=false){return this.json<DiagnosticsReport>('/diagnostics/check',{request_bluetooth_permission});}
  sources(){return this.json<{sources:Source[];blockers:string[]}>('/sources');}
  connect(source_id:string,device_model:string){return this.json<Status>('/connect',{source_id,device_model,consent:true});}
  disconnect(){return this.json<Status>('/disconnect',{});}
diff --git a/apps/training/src/interaction.ts b/apps/training/src/interaction.ts
index 5aa65f1..0799658 100644
--- a/apps/training/src/interaction.ts
+++ b/apps/training/src/interaction.ts
@@ -17,7 +17,7 @@ export class IntentMarkerController {
 export type CuePhase='ready'|'active'|'quiet'|'complete';
 export function cuePhase(start:number,now:number):CuePhase {const elapsed=now-start;return elapsed<2000?'ready':elapsed<4000?'active':elapsed<6000?'quiet':'complete';}
 export function validateReview(start:number,end:number,reviewer:string){if(!Number.isFinite(start)||!Number.isFinite(end)||start<0||end<=start)throw Error('Enter a valid device-time interval with end after start.');if(!reviewer.trim())throw Error('Add a reviewer name or initials.');}
-export function sourceLabel(mode:string|null|undefined,connected:boolean){if(!connected)return 'NO STREAM';return mode==='real_device'?'LIVE EEG':mode==='synthetic'?'SYNTHETIC REHEARSAL':'UNVERIFIED SOURCE';}
+export function sourceLabel(mode:string|null|undefined,connected:boolean,age:number|null=0){if(!connected)return 'NO STREAM';if(mode==='synthetic')return 'SYNTHETIC REHEARSAL';if(mode==='real_device')return age===null?'WAITING FOR EEG':age>.5?'EEG STALLED':'LIVE EEG';return 'UNVERIFIED SOURCE';}
 
 export function reviewSegmentBounds(start:number,end:number,recordedStart=0,recordedEnd=Infinity){
  if(!Number.isFinite(start)||!Number.isFinite(end)||end<=start)throw Error('Enter valid interval bounds before loading its trace.');
diff --git a/apps/training/src/main.tsx b/apps/training/src/main.tsx
index 9ac62c1..e7dc7de 100644
--- a/apps/training/src/main.tsx
+++ b/apps/training/src/main.tsx
@@ -3,6 +3,7 @@ import {createRoot} from 'react-dom/client';
 import {TrainingAPI,consumeTokenFragment} from './api';
 import {IntentMarkerController,TRIALS,cuePhase,sourceLabel,validateReview,reviewSegmentBounds} from './interaction';
 import {TracePlot} from './TracePlot';
+import {ConnectionDiagnostics} from './ConnectionDiagnostics';
 import type {Marker,ModelSummary,ReviewBundle,Role,Source,Status,Trace,TrialClass} from './types';
 import './style.css';
 const initialToken=consumeTokenFragment(window.location,window.history);
@@ -14,7 +15,7 @@ function App(){
  const statusRef=useRef(status),trialRef=useRef(trial),reviewGeneration=useRef(0),cueTimers=useRef<number[]>([]),initialAuth=useRef(false),statusGeneration=useRef(0);
  statusRef.current=status;trialRef.current=trial;
  const markerController=useRef<IntentMarkerController|null>(null);if(!markerController.current)markerController.current=new IntentMarkerController(marker=>api.current.marker(marker),()=>performance.now(),()=>crypto.randomUUID(),setHeld,problem=>setError(String(problem)));
- const recording=!!status?.recording,connected=!!status?.connected,model=status?.model??null,mode=sourceLabel(status?.mode,connected);
+ const recording=!!status?.recording,connected=!!status?.connected,model=status?.model??null,mode=sourceLabel(status?.mode,connected,status?.sample_age_s??null),flowing=connected&&status?.sample_age_s!=null&&status.sample_age_s<=.5;
  function cancelCue(){for(const timer of cueTimers.current)window.clearTimeout(timer);cueTimers.current=[];setCue(null);}
  async function run(label:string,action:()=>Promise<void>){statusGeneration.current++;setBusy(label);setError('');try{await action();}catch(problem){setError(problem instanceof Error?problem.message:String(problem));}finally{statusGeneration.current++;setBusy('');}}
  async function authenticate(){await run('service',async()=>{const next=await api.current.authenticate(token);setStatus(next);setAuthenticated(true);setToken('');setNotice('Local service connected. Choose an existing LSL source or explicit synthetic rehearsal.');});}
@@ -39,9 +40,10 @@ function App(){
  useEffect(()=>setMetadataConfirmed(false),[descriptorIdentity]);
  const activePhase=cue?cuePhase(cue.start,clock):null,selectedMarker=review?.markers.find(item=>item.id===markerId);
  return <main className="training-app"><header className="app-header"><a className="brand" href={studioURL}>SceneScore<span> / </span><strong>Blink training</strong></a><a className="studio-link" href={studioURL}>Open music studio <span>↗</span></a></header>
- <div className="page-title"><div><span className="eyebrow">LOCAL SIGNAL WORKSPACE</span><h1>Collect. Review. Train.</h1><p>Your raw EEG stays on this machine. Intent markers become examples only after review.</p></div><div className="mode-label"><i className={connected?'active':''}/><strong>{mode}</strong><span>{recording?'RECORDING LOCALLY':connected?'STREAMING · NOT RECORDING':'NOT RECORDING'}</span></div></div>
+ <div className="page-title"><div><span className="eyebrow">LOCAL SIGNAL WORKSPACE</span><h1>Collect. Review. Train.</h1><p>Your raw EEG stays on this machine. Intent markers become examples only after review.</p></div><div className="mode-label"><i className={flowing?'active':connected?'waiting':''}/><strong>{mode}</strong><span>{recording?'RECORDING LOCALLY':flowing?'STREAMING · NOT RECORDING':connected?'WAITING FOR SAMPLES':'NOT RECORDING'}</span></div></div>
  <section className="service-strip" aria-label="Local service connection"><span className="section-number">01</span><div className="service-state"><strong>Local training service</strong><span>127.0.0.1:8767 · {authenticated?(status?'authenticated session':'connection unavailable · reconnect'):'not connected'}</span></div><label>Training service token<input aria-label="Training service token" type="password" autoComplete="off" placeholder="Paste the local session token" value={token} onChange={event=>setToken(event.target.value)}/></label><button onClick={()=>void authenticate()} disabled={!!busy||!token.trim()}>{authenticated?'Reconnect service':'Connect service'}</button></section>{!status&&<p className="setup-help">Open <strong>Launch Blink Trainer.command</strong> in SceneScore, or run <code>make muse-train</code>. It starts the local service and opens this page with authentication. If opened separately, paste the token printed by that service. On the hosted page, allow the browser’s Local network access prompt, or use the local page opened by the launcher.</p>}
  <div className="notice" role="status">{notice}</div>{error&&<div className="error" role="alert"><strong>Action needed</strong><span>{error}</span><button onClick={()=>setError('')} aria-label="Dismiss error">×</button></div>}
+ <ConnectionDiagnostics api={api.current} authenticated={authenticated} serviceAvailable={!!status} onSources={setSources}/>
  <div className="capture-workspace"><aside className="capture-settings"><div className="section-heading"><span className="section-number">02</span><h2>Source & recording</h2></div><label>EEG source<select aria-label="EEG source" value={sourceId} disabled={connected||!!busy} onChange={event=>{setSourceId(event.target.value);setMetadataConfirmed(false);}}><option value="">Select a source</option><option value="synthetic">Synthetic rehearsal · no headset</option>{sources.map(source=><option value={source.id} key={source.id}>{source.name} · {source.sample_rate_hz} Hz</option>)}</select></label><button className="text-button" onClick={()=>void discover()} disabled={!authenticated||connected||!!busy}>Find / refresh LSL streams</button>{blockers.length>0&&<p className="field-help">{blockers.join(' · ')}</p>}<p className="field-help">For Muse, start the local Muse-to-LSL stream first. The page connects to that existing stream; it does not turn on Bluetooth or substitute a simulated source.</p>{sourceId!=='synthetic'&&<label>Device model<input aria-label="Device model" value={deviceModel} disabled={connected} placeholder="Your verified Muse model" onChange={event=>{setDeviceModel(event.target.value);setMetadataConfirmed(false);}}/></label>}<label className="check"><input type="checkbox" checked={processingConsent} disabled={connected} onChange={event=>setProcessingConsent(event.target.checked)}/><span>I consent to processing this source locally. This does not start recording.</span></label><div className="button-row"><button className="primary" disabled={!authenticated||connected||!processingConsent||!sourceId||!!busy} onClick={()=>void connect()}>Connect source</button><button disabled={!connected||!!busy} onClick={()=>void disconnect()}>Disconnect</button></div>
  <div className="settings-divider"/><div className="two-fields"><label>Participant ID<input aria-label="Participant ID" placeholder="e.g. player01" value={participant} disabled={recording} onChange={event=>setParticipant(event.target.value)}/></label><label>Refit ID<input aria-label="Refit ID" placeholder="e.g. fit01" value={refit} disabled={recording} onChange={event=>setRefit(event.target.value)}/></label></div><label>Session role<select aria-label="Session role" value={role} disabled={recording} onChange={event=>setRole(event.target.value as Role)}><option value="train">Train · fit model</option><option value="development">Development · held out</option><option value="final_test">Final test · kept separate</option></select></label><p className="field-help">Assign each complete refit to one role. Remove and refit the headset for an independent session.</p><label>Record seconds<input aria-label="Record seconds" type="number" min="10" max="600" value={seconds} disabled={recording} onChange={event=>setSeconds(Number(event.target.value))}/></label><label>Recording consent statement<textarea aria-label="Recording consent statement" rows={3} placeholder="In your own words: frontal EEG and timestamps, local storage and retention, stopping and deletion." value={consentStatement} disabled={recording} onChange={event=>setConsentStatement(event.target.value)}/></label><label className="check"><input type="checkbox" checked={storageConsent} disabled={recording} onChange={event=>setStorageConsent(event.target.checked)}/><span>I consent to saving raw EEG, timestamps and markers locally. I can stop and request deletion, including derived files.</span></label>{connected&&status?.mode==='real_device'&&<label className="check"><input aria-label="Confirm Muse stream metadata" type="checkbox" checked={metadataConfirmed} disabled={recording} onChange={event=>setMetadataConfirmed(event.target.checked)}/><span>I confirm this selected stream comes from my named Muse and the displayed channel names, units, and sample rate match it.</span></label>}<button className={recording?'stop-button':'primary record-button'} disabled={!!busy||(!recording&&(!connected||!storageConsent||!consentStatement.trim()||(status?.mode==='real_device'&&!metadataConfirmed)))} onClick={()=>void(recording?stopRecording():startRecording())}>{recording?'Stop recording':'Start recording'}</button><p className="field-help">Stop on discomfort. Recording consent does not permit uploading or publication.</p></aside>
  <section className="signal-workspace" aria-label="Signal and intent marking"><div className="signal-top"><div><h2>Frontal EEG</h2><p>{status?.channels.length?status.channels.map(channel=>channel.name+' · '+channel.unit).join(' / '):'Waiting for a verified channel list'}</p></div><div className="signal-numbers"><span><b>{status?.sample_rate_hz??'—'}</b> nominal Hz</span><span><b>{status?.measured_rate_hz?.toFixed(1)??'—'}</b> measured Hz</span><span><b>{status?.sample_count??0}</b> samples</span></div></div><TracePlot trace={trace} held={held}/><div className="signal-status"><span>Signal quality: <strong>{qualityText(status?.quality)}</strong></span><span>{status?.reason??'No stream connected'}</span></div>
diff --git a/apps/training/src/style.css b/apps/training/src/style.css
index 0004645..6e27112 100644
--- a/apps/training/src/style.css
+++ b/apps/training/src/style.css
@@ -1,3 +1,9 @@
 :root{font-family:Inter,ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#18302c;background:#f7f8f4;font-synthesis:none;font-weight:400}*{box-sizing:border-box}body{margin:0}button,input,select,textarea{font:inherit}button,a,input,select,textarea,summary{-webkit-tap-highlight-color:transparent}a{color:inherit;text-decoration:none}button{min-height:38px;padding:9px 14px;background:transparent;border:1px solid #bccdc5;border-radius:4px;font-size:12px;line-height:1.35;color:#19352e;cursor:pointer;transition:background .14s,border-color .14s}button:hover:not(:disabled){background:#eaf1ec;border-color:#608c7e}button:disabled{opacity:.42;cursor:not-allowed}.primary{color:white;background:#14705e;border-color:#14705e}.primary:hover:not(:disabled){background:#0e5e4e;border-color:#0e5e4e}.text-button{padding:5px 0;border:0;text-align:left;min-height:26px;color:#14705e}.stop-button{border-color:#193b31;background:#193b31;color:white;width:100%;font-weight:600}.record-button{width:100%}button:focus-visible,a:focus-visible,input:focus-visible,select:focus-visible,textarea:focus-visible,summary:focus-visible{outline:2px solid #16826c;outline-offset:3px}label{display:flex;flex-direction:column;gap:7px;font-size:11px;font-weight:600;line-height:1.4;color:#355047}input:not([type=checkbox]),select,textarea{border:1px solid #c9d7ce;background:#fffefb;border-radius:4px;padding:9px 10px;min-height:38px;color:#19392e;width:100%;font-weight:400;font-size:12px}textarea{resize:vertical;line-height:1.5}input::placeholder,textarea::placeholder{color:#8b9a90}input:disabled,select:disabled,textarea:disabled{background:#eef2ec;color:#7f8d83}.training-app{max-width:1500px;margin:auto;padding:0 42px}.app-header{height:82px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #d4dfd4}.brand{font-size:19px;font-weight:700;letter-spacing:-.7px}.brand span{font-weight:400;color:#bac7bd;padding:0 9px}.brand strong{font-size:15px;font-weight:500;letter-spacing:-.2px}.studio-link{font-size:12px;color:#486458}.studio-link span{margin-left:14px;color:#16826c;font-size:18px}.page-title{display:flex;justify-content:space-between;align-items:center;gap:30px;padding:36px 0 28px}.eyebrow{font-size:9px;font-weight:700;letter-spacing:.15em;color:#688175;line-height:1.5}h1{font-size:34px;line-height:1.2;font-weight:600;letter-spacing:-1.1px;margin:9px 0 9px}.page-title p,.section-heading p{font-size:12px;line-height:1.6;color:#718478;margin:0}.mode-label{display:grid;grid-template-columns:10px 1fr;column-gap:9px;row-gap:8px;white-space:nowrap}.mode-label i{width:7px;height:7px;border-radius:50%;background:#bbc8bc;margin-top:3px}.mode-label i.active{background:#16826c}.mode-label strong{font-size:10px;letter-spacing:.06em}.mode-label span{font-size:9px;letter-spacing:.05em;color:#758a7c;grid-column:2}.service-strip{display:flex;align-items:center;gap:20px;border-top:1px solid #d1ded2;border-bottom:1px solid #d1ded2;padding:18px 0}.section-number{font-family:ui-monospace,SFMono-Regular,monospace;font-size:10px;color:#5a8c76;flex:0 0 auto}.service-state{display:flex;flex-direction:column;gap:7px;min-width:250px}.service-state strong{font-size:12px;font-weight:600}.service-state span{font-family:ui-monospace,SFMono-Regular,monospace;color:#859589;font-size:10px}.service-strip label{margin-left:auto;flex:0 1 350px}.service-strip button{align-self:flex-end}.notice{font-size:11px;line-height:1.7;color:#627b6b;padding:13px 0;min-height:44px}.error{border:1px solid #b0b9a5;background:#f2f1e8;padding:11px 14px;display:flex;gap:16px;align-items:center;font-size:12px;line-height:1.6;margin:2px 0 20px}.error strong{white-space:nowrap}.error button{border:0;min-height:24px;margin-left:auto;padding:0 5px;font-size:20px}.capture-workspace{display:grid;grid-template-columns:276px minmax(0,1fr);gap:38px;align-items:start}.capture-settings{border-right:1px solid #d6e0d4;padding-right:27px;display:flex;flex-direction:column;gap:13px}.section-heading{display:flex;gap:13px;align-items:flex-start;margin-bottom:23px}.section-heading .section-number{margin-top:6px}.section-heading h2,h2{font-size:17px;letter-spacing:-.35px;font-weight:600;line-height:1.4;margin:0}.capture-settings .section-heading{margin-bottom:5px}.field-help{font-size:10px;color:#809083;line-height:1.65;margin:0}.check{flex-direction:row;align-items:flex-start;font-size:10px;font-weight:400;line-height:1.65;color:#697f70}.check input{margin:3px 1px 0 0;accent-color:#14705e}.button-row{display:flex;gap:7px;flex-wrap:wrap}.two-fields{display:grid;grid-template-columns:1fr 1fr;gap:10px}.settings-divider{height:1px;background:#d6e0d4;margin:7px 0 5px}.signal-top{display:flex;justify-content:space-between;gap:15px;align-items:flex-start;margin-bottom:18px}.signal-top p{font-size:10px;color:#84978a;margin:7px 0 0}.signal-numbers{display:flex;gap:18px;color:#90a092;font-size:9px;padding-top:3px}.signal-numbers b{font-family:ui-monospace,SFMono-Regular,monospace;font-weight:400;color:#486758;display:block;font-size:14px;margin-bottom:5px}.signal-plot{background:#fffefb;border:1px solid #d2dfd1;border-radius:5px;overflow:hidden;transition:background .15s}.signal-plot.is-held{background:#eaf6ec}.plot-heading{display:flex;justify-content:space-between;font-size:9px;letter-spacing:.07em;padding:15px 17px;color:#537a63;border-bottom:1px solid #e5ece1}.plot-heading small{font-size:9px;letter-spacing:0;text-transform:none;margin-left:7px;color:#97a791}.plot-heading>span:last-child{letter-spacing:0;color:#8ca081}.plot-body{display:grid;grid-template-columns:85px minmax(0,1fr);padding:15px 16px 4px 0}.channel-labels{display:flex;flex-direction:column}.channel-labels>div{height:92px;display:flex;justify-content:center;flex-direction:column;padding-left:17px;gap:5px}.channel-labels strong{font-size:11px;font-weight:600;color:#3e6c51}.channel-labels span,.channel-labels small{font-family:ui-monospace,SFMono-Regular,monospace;font-size:8px;color:#9aaa91}.plot-body svg{display:block;width:100%;overflow:hidden}.plot-grid{stroke:#eef3e8;stroke-width:1}.plot-zero{stroke:#dae5d4;stroke-width:1;stroke-dasharray:3 3}.signal-path{fill:none;stroke:#168164;stroke-width:1.3;vector-effect:non-scaling-stroke;stroke-linecap:round;stroke-linejoin:round}.review-selection{fill:#38a77b;opacity:.15;pointer-events:none}.plot-time{display:flex;justify-content:space-between;gap:6px;font-family:ui-monospace,SFMono-Regular,monospace;font-size:8px;padding:6px 16px 13px 85px;color:#9aaa91}.plot-time>span:nth-child(2){max-width:65%;text-overflow:ellipsis;overflow:hidden;white-space:nowrap}.plot-foot{border-top:1px solid #e6ece1;font-size:8px;color:#8ca181;display:flex;justify-content:space-between;gap:10px;padding:11px 17px}.signal-empty{display:flex;align-items:center;justify-content:center;flex-direction:column;min-height:266px;text-align:center;padding:25px}.signal-empty svg{width:180px;height:40px;fill:none;stroke:#e4ece0;stroke-width:1}.signal-empty p{font-size:12px;color:#84987b;margin:19px 0 6px}.signal-empty>span{font-size:10px;color:#a0b195}.signal-status{display:flex;justify-content:space-between;gap:20px;font-size:10px;color:#8a9e7f;margin-top:12px}.signal-status strong{color:#59734b;font-weight:500}.trial-workspace{display:grid;grid-template-columns:1fr 1fr;gap:32px;margin-top:34px;padding-top:25px;border-top:1px solid #d5e1cd}.trial-controls{display:flex;flex-direction:column;gap:13px}.trial-controls>label{max-width:240px}.key-instruction{display:flex;gap:13px;align-items:center;margin:5px 0;transition:color .15s}.key-instruction kbd{font-family:inherit;background:#fffffb;border:1px solid #b7c8ae;box-shadow:0 2px 0 #d4deca;border-radius:5px;font-size:24px;width:47px;height:48px;text-align:center;line-height:45px;color:#2c543e;transition:transform .12s,background .12s}.key-instruction.held kbd{transform:translateY(2px);box-shadow:none;background:#d4efdc}.key-instruction>div{display:flex;flex-direction:column;gap:6px}.key-instruction strong{font-size:11px;font-weight:600;color:#416340}.key-instruction span{font-size:9px;color:#8a9e7c;line-height:1.5}.cue-panel{border-left:1px solid #d7e1cc;padding-left:27px}.cue-display{min-height:133px;display:flex;flex-direction:column;justify-content:center;gap:13px}.cue-display strong{font-size:19px;font-weight:500;line-height:1.35;letter-spacing:-.4px;color:#345732}.cue-display span{font-size:10px;line-height:1.6;color:#8aa175}.cue-panel>button{margin-bottom:13px}.prediction-line{margin-top:28px;border-top:1px solid #d5e1c9;border-bottom:1px solid #d5e1c9;display:grid;grid-template-columns:1fr auto;gap:13px;padding:17px 0}.prediction-line>strong{font-family:ui-monospace,SFMono-Regular,monospace;font-size:16px;font-weight:400;color:#527146}.prediction-line>span:last-child{grid-column:1/-1;font-size:10px;color:#92a780;line-height:1.6}.review-workspace,.model-workspace{margin-top:43px;padding-top:31px;border-top:1px solid #d3dfc5}.review-workspace>.section-heading,.model-workspace>.section-heading{align-items:flex-start}.section-heading>label,.section-heading>button{margin-left:auto;min-width:270px}.section-heading>label{max-width:430px}.section-heading>button{min-width:130px}.section-heading p{margin-top:7px;max-width:650px}.review-empty{display:flex;justify-content:space-between;gap:30px;padding:30px 0;color:#748d61;font-size:12px}.review-empty p{font-size:10px;line-height:1.7;max-width:460px;margin:0;color:#91a37e}.review-toolbar{display:flex;align-items:flex-end;gap:25px;margin:0 0 18px}.review-toolbar>label{width:370px;max-width:50%}.review-toolbar>span{font-size:10px;color:#789961;margin-bottom:10px}.review-toolbar .button-row{margin-left:auto}.review-form{display:grid;grid-template-columns:1fr 1fr 1fr;gap:27px;padding:23px 0 10px}.review-form>div{display:flex;flex-direction:column;gap:13px}.review-form>div>button{align-self:flex-start}.delete-session{border-top:1px solid #d5e0c6;margin-top:18px;padding-top:15px;font-size:11px;color:#718d5a}.delete-session summary{cursor:pointer;font-size:11px}.delete-session p{font-size:10px;line-height:1.6}.delete-session label{max-width:450px;margin:12px 0}.model-columns{display:grid;grid-template-columns:1fr 1fr;gap:55px}.model-columns>div:nth-child(2){border-left:1px solid #d5dfc4;padding-left:35px}h3{font-size:20px;font-weight:500;letter-spacing:-.45px;margin:13px 0 10px;color:#325b35}.model-columns p{font-size:11px;line-height:1.7;color:#819568;max-width:490px}.model-details{display:grid;grid-template-columns:130px 1fr;gap:10px;font-size:10px;margin:23px 0}.model-details dt{color:#94a67a}.model-details dd{margin:0;color:#517744;overflow-wrap:anywhere}.model-empty{padding:25px 0 10px}.model-empty strong{font-size:14px;font-weight:500;color:#5a7b46}.metric-report{display:grid;grid-template-columns:1fr 1fr;gap:15px 25px;margin:22px 0}.metric-report>div{display:flex;flex-direction:column;gap:7px;min-width:0}.metric-report span{font-size:10px;color:#8ca477;text-transform:capitalize}.metric-report strong{font-family:ui-monospace,SFMono-Regular,monospace;font-size:13px;font-weight:400;color:#497239;overflow-wrap:anywhere}.model-workspace details{font-size:11px;color:#789b5c;margin-top:20px}.model-workspace summary{cursor:pointer}pre{font-size:10px;background:#f0f4e7;line-height:1.6;padding:13px;overflow:auto;max-height:340px}footer{padding:32px 0 25px;margin-top:40px;border-top:1px solid #d9e2c8;display:flex;justify-content:space-between;gap:30px;font-size:8px;color:#99ad7f;letter-spacing:.05em}footer span:last-child{letter-spacing:0}@media(min-width:1600px){.training-app{max-width:1550px}}@media(max-width:1100px){.training-app{padding:0 25px}.capture-workspace{grid-template-columns:245px minmax(0,1fr);gap:25px}.capture-settings{padding-right:22px}.signal-numbers{gap:10px}.trial-workspace{gap:20px}.cue-panel{padding-left:20px}.service-state{min-width:200px}.section-heading>label{min-width:230px}.review-toolbar{flex-wrap:wrap;gap:15px}.review-toolbar .button-row{margin-left:0}.review-form{gap:18px}.plot-foot>span:last-child{display:none}}@media(max-width:780px){.training-app{padding:0 18px}.app-header{height:65px}.brand{font-size:16px}.brand strong{font-size:12px}.studio-link{font-size:10px}.page-title{align-items:flex-start;padding:28px 0 22px}.page-title h1{font-size:27px}.page-title p{font-size:11px;max-width:300px}.mode-label{margin-top:9px;white-space:normal}.mode-label span{max-width:150px;line-height:1.5}.service-strip{flex-wrap:wrap;gap:12px}.service-strip label{margin-left:0;flex:1 1 210px}.service-state{flex:1}.capture-workspace{display:flex;flex-direction:column;gap:25px}.capture-settings{width:100%;border:0;padding:0;display:grid;grid-template-columns:1fr 1fr;gap:13px 18px}.capture-settings>.section-heading,.capture-settings>.field-help,.capture-settings>.check,.capture-settings>.settings-divider,.capture-settings>.record-button,.capture-settings>.stop-button{grid-column:1/-1}.capture-settings>.settings-divider{width:100%}.capture-settings>.text-button{align-self:end}.capture-settings>.button-row{align-self:end}.capture-settings>.two-fields{grid-column:1/-1}.signal-workspace{width:100%}.signal-top{border-top:1px solid #d6e1ca;padding-top:25px}.trial-workspace{gap:22px}.review-workspace>.section-heading{flex-wrap:wrap}.section-heading>label{margin-left:23px;max-width:none;width:100%}.review-form{grid-template-columns:1fr 1fr}.review-form>div:last-child{grid-column:1/-1}.review-toolbar>label{max-width:100%;width:100%}.model-columns{gap:25px}.model-columns>div:nth-child(2){padding-left:22px}.model-workspace>.section-heading>button{min-width:108px}.signal-empty{min-height:215px}.review-empty{display:block}.review-empty p{margin-top:12px}footer{line-height:1.6}.model-details{grid-template-columns:105px 1fr}}@media(max-width:500px){.page-title{flex-direction:column;gap:8px}.mode-label{margin-top:0}.mode-label span{max-width:none}.brand span{padding:0 4px}.brand strong{font-size:11px}.studio-link{font-size:9px}.studio-link span{margin-left:4px}.signal-numbers{font-size:8px;gap:9px}.signal-numbers b{font-size:12px}.signal-top h2{font-size:15px}.trial-workspace,.review-form,.model-columns{grid-template-columns:1fr}.cue-panel,.model-columns>div:nth-child(2){border-left:0;padding-left:0;border-top:1px solid #d5dfc4;padding-top:22px}.cue-display{min-height:100px}.review-form>div:last-child{grid-column:auto}.capture-settings{grid-template-columns:1fr}.capture-settings>*{grid-column:1!important}.signal-status{flex-direction:column;gap:5px}.plot-body{grid-template-columns:65px minmax(0,1fr)}.channel-labels>div{padding-left:10px}.plot-time{padding-left:65px}.plot-time>span:nth-child(2){display:none}.model-workspace>.section-heading{flex-wrap:wrap}.model-workspace>.section-heading>button{margin-left:23px}.prediction-line>.eyebrow{max-width:210px}footer{flex-direction:column;gap:8px}.service-strip>.section-number{display:none}.service-state{min-width:100%}}@media(prefers-reduced-motion:reduce){*,*::before,*::after{transition:none!important;scroll-behavior:auto!important}.key-instruction.held kbd{transform:none}}
 
 .setup-help{font-size:11px;line-height:1.7;color:#72836f;margin:15px 0 0;max-width:900px}.setup-help strong{font-weight:600;color:#49684e}.setup-help code{background:#edf2e8;color:#42673f;border-radius:3px;padding:2px 5px;font-size:10px}
+
+.connection-diagnostics{margin:20px 0 35px;border-top:1px solid #d6e1ca;border-bottom:1px solid #d6e1ca;padding:22px 0;font-size:12px}.diagnostics-heading{display:flex;align-items:center;gap:14px}.diagnostics-heading>div{flex:1}.diagnostics-heading h2{font-size:18px;margin:0 0 8px}.diagnostics-heading p{color:#526f64;max-width:750px;line-height:1.6}.diagnostics-heading .check{flex-shrink:0}.diagnostics-clock{display:flex;justify-content:space-between;gap:14px;color:#61796b;font-size:10px;margin:15px 0}.diagnostic-stages{list-style:none;padding:0;margin:0}.diagnostic-stages li{display:grid;grid-template-columns:24px 1fr 84px;gap:13px;align-items:start;padding:12px 0;border-top:1px solid #e1e8dc}.stage-number{color:#7b9282;font:11px monospace;padding-top:2px}.diagnostic-stages strong{font-size:12px}.diagnostic-stages p{color:#496656;margin:5px 0;line-height:1.5}.diagnostic-stages small{display:block;color:#617363;line-height:1.5;font-size:11px}.stage-state{text-align:right;font-size:10px;color:#6b7f71;text-transform:uppercase;letter-spacing:.08em}.diagnostic-stages li[data-state=pass] .stage-state{color:#147966}.diagnostic-stages li[data-state=blocked] .stage-state{color:#a34232}.diagnostic-stages li[data-state=warning] .stage-state{color:#91631f}.diagnostics-signal{display:flex;gap:10px 22px;flex-wrap:wrap;padding:15px 0;color:#496956;font:11px monospace}.diagnostics-actions{display:flex;align-items:center;gap:12px;padding:15px 0;flex-wrap:wrap}.diagnostics-actions>span{color:#6b7b6e;font-size:10px;line-height:1.6}.diagnostics-empty{padding:20px 0;color:#758571;line-height:1.6}.connection-diagnostics details{margin-top:10px;line-height:1.7;color:#526a59}.diagnostics-events{list-style:none;padding:0;max-height:270px;overflow:auto}.diagnostics-events li{display:grid;grid-template-columns:75px 190px 1fr;gap:12px;border-top:1px solid #e1e8dc;padding:8px 0;font-size:10px}.diagnostics-events time{font-family:monospace;color:#788774}@media(max-width:700px){.diagnostics-heading{flex-wrap:wrap}.diagnostics-heading>div{flex-basis:100%}.diagnostics-clock{flex-direction:column;gap:5px}.diagnostic-stages li{grid-template-columns:18px 1fr 68px;gap:8px}.diagnostics-events li{grid-template-columns:65px 1fr}.diagnostics-events li>span{grid-column:1/-1}}
+
+.mode-label i.waiting{background:#b08233}
+
+.diagnostic-stages li>div,.diagnostics-events li>span{min-width:0;overflow-wrap:anywhere}
diff --git a/apps/training/src/types.ts b/apps/training/src/types.ts
index 5bfff13..bddbcfe 100644
--- a/apps/training/src/types.ts
+++ b/apps/training/src/types.ts
@@ -5,10 +5,20 @@ export interface Channel {name:string;unit:string}
 export type Quality=string|{state:string;reason?:string|null};
 export interface SessionSummary {id:string;participant_id:string;refit_id:string;role:Role;source_mode:SourceMode;samples:number;duration_s:number;marker_count:number;reviewed_count:number;status:string;start_s?:number;end_s?:number}
 export interface ModelSummary {id:string;status:string;positive_examples:number;negative_examples:number;source_mode:SourceMode;held_out:unknown;model_kind:string;control_authority:false;[key:string]:unknown}
-export interface Status {version:string;connected:boolean;mode:SourceMode|null;recording:boolean;source:unknown;channels:Channel[];sample_rate_hz:number|null;measured_rate_hz:number|null;sample_count:number;last_device_s:number|null;device_epoch:string|null;quality:Quality;reason:string|null;current_session_id:string|null;sessions:SessionSummary[];model:ModelSummary|null}
+export interface Status {version:string;connected:boolean;mode:SourceMode|null;recording:boolean;source:unknown;channels:Channel[];sample_rate_hz:number|null;measured_rate_hz:number|null;sample_count:number;last_device_s:number|null;sample_age_s?:number|null;device_epoch:string|null;quality:Quality;reason:string|null;current_session_id:string|null;sessions:SessionSummary[];model:ModelSummary|null}
 export interface Trace {channels:Channel[];times_s:number[];samples:number[][];device_epoch:string|null;source_mode:SourceMode|null;quality:Quality;prediction:{score:number;model_id:string;semantics:string}|null}
 export interface Source {id:string;name:string;channels:Channel[];sample_rate_hz:number}
 export interface Marker {id:string;class_name:TrialClass;start_s:number;end_s:number|null;source:string;review_required:true;[key:string]:unknown}
 export interface Review {marker_id:string;start_s:number;end_s:number;class_name:TrialClass;certainty:'reviewed'|'uncertain';reviewer:string;notes:string;[key:string]:unknown}
 export interface ReviewBundle {session:SessionSummary;markers:Marker[];reviews:Review[]}
 export interface MarkerRequest {event:'down'|'up'|'cue';id:string;class_name:TrialClass;client_ms:number}
+export type DiagnosticState='pass'|'warning'|'blocked'|'unknown';
+export interface DiagnosticsReport {
+ version:string;enabled:boolean;checking:boolean;checked_at:string|null;check_age_s:number|null;
+ stages:{id:string;label:string;state:DiagnosticState;detail:string;action:string}[];
+ signal:{age_s:number|null;window_s:number;gaps:number;rate_hz:number|null;channels:{name:string;unit:string;min:number;max:number;peak_to_peak:number;flat:boolean}[]};
+ discovery:{sources:Source[];blockers:string[];observed:{name:string;id:string;error?:string}[]};
+ bluetooth:{authorization?:string;power?:string;devices?:{id:string;name:string;system_connected:boolean;advertising:boolean;rssi_dbm?:number}[];error?:string};
+ events:{at:string;stage:string;state:DiagnosticState;detail:string}[];source_mode:SourceMode|null;
+ configuration:{transport:string;board_id:string;units:string;env_placeholders_used:boolean};
+}
diff --git a/docs/MUSE_TRAINING.md b/docs/MUSE_TRAINING.md
index 026f415..85296bd 100644
--- a/docs/MUSE_TRAINING.md
+++ b/docs/MUSE_TRAINING.md
@@ -80,3 +80,36 @@ a new session rather than hiding a dropout in existing data.
 Developer build: `node_modules/.bin/vite build apps/training --base ./ --outDir ../../artifacts/training-dist`.
 The webpage is a separate app and does not rebuild the concurrently evolving
 music/Blender demonstration.
+
+## Automatic connection diagnostics
+
+After local authentication, the page checks Bluetooth/Muse-service and LSL
+metadata every ten seconds, and sample health every second. **Check now** forces
+a refresh; **Automatic checks** pauses hardware discovery. No check pairs the
+headset, connects an EEG inlet or starts a recording. Use the separate source
+connection and recording controls for those actions.
+
+The checklist separates local service, Bluetooth power/permission, a Muse-service
+advertisement or macOS connection, an LSL EEG outlet and valid descriptor, the
+selected inlet, arriving samples, and signal continuity. Each failure includes a
+next action. Timestamped changes appear in connection history. Diagnostic reports
+can be downloaded locally; they contain metadata and summary values, no raw EEG
+samples or tokens. A changing signal alone does not verify electrode contact.
+
+The local launcher builds a small macOS CoreBluetooth metadata helper. If macOS
+permission has not been requested, click **Enable Bluetooth check**, then respond
+to the OS prompt. If permission was denied, enable Bluetooth for the app running
+the launcher (usually Terminal) in System Settings → Privacy & Security → Bluetooth.
+An LSL stream can still work when independent Bluetooth inspection is unavailable.
+No visible Muse advertisement is not proof of disconnection; the headset may
+already be in use, not advertising, or outside this bounded service scan.
+
+`MUSE_TRANSPORT`, `MUSE_BOARD_ID`, and `MUSE_UNITS` in `.env` are unused placeholders
+in this trainer. Leave them blank: transport is LSL, no BrainFlow board ID is used,
+and channel units come from the source descriptor (frontal channels require µV).
+A BrainFlow board ID selects its hardware driver, not a particular headset's
+Bluetooth address: see the [BrainFlow API](https://brainflow.readthedocs.io/en/stable/UserAPI.html).
+The distinction between LSL outlets and actual sample receipt is described by the
+[LSL user guide](https://labstreaminglayer.readthedocs.io/info/user_guide.html).
+Muse-service discovery uses the FE8D service identified by
+[MuseLSL](https://github.com/alexandrebarachant/muse-lsl/blob/master/muselsl/constants.py).
diff --git a/services/training/README.md b/services/training/README.md
index 09b2120..e6fcb4e 100644
--- a/services/training/README.md
+++ b/services/training/README.md
@@ -19,6 +19,31 @@ Origin and cross-site Fetch Metadata. Requests are bounded to 16 KiB,120/10 seco
 Static access stays within the selected web root; dot paths, hidden targets and
 outside symlinks are rejected. No cookies, uploads or public raw endpoints exist.
 
+`GET /v1/diagnostics` provides timestamped connection evidence and raw-signal
+summaries, with no raw sample arrays. `POST /v1/diagnostics/monitor {enabled:true}`
+starts metadata discovery every ten seconds while the browser polls; disabling it
+or losing diagnostic polls for five seconds stops the worker. `/diagnostics/check`
+requests an immediate check. Its optional `request_bluetooth_permission:true`
+flag is sent only by the explicit Enable Bluetooth check button. Discovery never
+selects an inlet, pairs the headset or records data.
+
+The launcher compiles `tools/muse_bluetooth.swift` into an ignored local helper
+with its Bluetooth usage description. It only observes FE8D service advertising
+and system-connected peripherals. Each helper process is bounded, its exact
+identity retained, then reaped; shutdown terminates an active helper and joins
+the diagnostic thread. Streaming disables active BLE scans while retaining the
+system-connection query. The metadata monitor serializes bounded LSL discovery
+without putting it in the acquisition lock or audio path. Invalid EEG outlets
+remain visible as descriptor failures; non-EEG outlets are ignored.
+
+Checks separately report permission/power, device observations, LSL metadata,
+selected inlet, sample receipt age and recent continuity/rate/variation. Stale
+hardware observations expire after twenty seconds. Freshness is a diagnostic
+0.5-second batch-age check; source timeout stays two seconds and rate mismatch
+uses the existing 2% recording criterion. Neither varying data nor BLE discovery
+certifies electrode contact or a physical Muse identity. `.env`'s MUSE_TRANSPORT,
+MUSE_BOARD_ID and MUSE_UNITS are unused by this LSL trainer.
+
 Endpoints match `docs/requests/muse-training-api.md`. Source connection and
 recording have separate consent actions. Real discovery opens a bounded LSL
 inlet to inspect `info()`'s full descriptor, then closes it. Selection is by
diff --git a/services/training/server.py b/services/training/server.py
index baad5aa..ee5c8e1 100644
--- a/services/training/server.py
+++ b/services/training/server.py
@@ -188,10 +188,13 @@ class Handler(BaseHTTPRequestHandler):
                     "/review": state.review,
                     "/train": lambda _: state.train(),
                     "/delete": state.delete,
+                    "/diagnostics/monitor": state.diagnostics.monitor,
+                    "/diagnostics/check": state.diagnostics.check_now,
                 }
             else:
                 handlers = {
                     "/status": lambda _: state.status(),
+                    "/diagnostics": lambda _: state.diagnostics.snapshot(),
                     "/sources": lambda _: state.sources.discover(),
                     "/trace": lambda _: state.trace(),
                     "/review": lambda b: state.review_get(b["session_id"]),
diff --git a/services/training/sources.py b/services/training/sources.py
index e9a0702..5dadcd7 100644
--- a/services/training/sources.py
+++ b/services/training/sources.py
@@ -2,6 +2,7 @@
 
 import math
 import time
+import threading
 
 
 def normalized_unit(value):
@@ -108,19 +109,32 @@ class SyntheticSource:
 class Sources:
     def __init__(self):
         self.known = {}
+        self.lock = threading.Lock()
+        self.discovery_lock = threading.Lock()
 
     def discover(self):
-        sources, blockers, seen, duplicates = [], [], set(), set()
+        with self.discovery_lock:
+            return self._discover()
+
+    def _discover(self):
+        sources, blockers, observed, seen, duplicates = [], [], [], set(), set()
         try:
             import pylsl
 
             advertised = pylsl.resolve_streams(wait_time=1)
-            if len(advertised) > 8:
-                raise ValueError("discovery_capacity_exceeded")
-            for item in advertised:
-                inlet = pylsl.StreamInlet(item, max_buflen=1, recover=False)
+            eeg = [item for item in advertised if not hasattr(item, "type") or item.type().lower() == "eeg"]
+            if len(eeg) > 8:
+                blockers.append("More than eight EEG outlets; narrow the streaming setup.")
+            deadline = time.monotonic() + 3
+            for item in eeg[:8]:
+                if time.monotonic() >= deadline:
+                    blockers.append("Descriptor discovery time budget reached; retry.")
+                    break
+                inlet = None
+                observation = {"name": item.name()[:80], "id": item.source_id()[:256]}
                 try:
-                    value = descriptor(inlet.info(timeout=1))
+                    inlet = pylsl.StreamInlet(item, max_buflen=1, recover=False)
+                    value = descriptor(inlet.info(timeout=min(1, max(0.01, deadline - time.monotonic()))))
                     if value["id"] in seen:
                         duplicates.add(value["id"])
                         raise ValueError("duplicate_source_id")
@@ -128,19 +142,25 @@ class Sources:
                     sources.append(value)
                 except (ValueError, RuntimeError, OSError) as exc:
                     blockers.append(str(exc))
+                    observation["error"] = str(exc)
                 finally:
-                    inlet.close_stream()
+                    observed.append(observation)
+                    if inlet is not None:
+                        inlet.close_stream()
         except (ImportError, ValueError, RuntimeError, OSError) as exc:
             blockers.append("LSL_unavailable:" + str(exc))
         sources = [s for s in sources if s["id"] not in duplicates]
-        self.known = {s["id"]: s for s in sources}
+        with self.lock:
+            self.known = {s["id"]: s for s in sources}
         if not sources and not blockers:
             blockers.append("No advertised verified-descriptor LSL source found")
-        return {"sources": sources, "blockers": blockers}
+        return {"sources": sources, "blockers": blockers, "observed": observed}
 
     def open(self, source_id):
         if source_id == "synthetic":
             return SyntheticSource()
-        if source_id not in self.known:
-            raise ValueError("select_a_discovered_source_first")
-        return LSLSource(source_id, self.known[source_id])
+        with self.lock:
+            if source_id not in self.known:
+                raise ValueError("select_a_discovered_source_first")
+            expected = self.known[source_id]
+        return LSLSource(source_id, expected)
diff --git a/services/training/state.py b/services/training/state.py
index bffafdb..a90d7fe 100644
--- a/services/training/state.py
+++ b/services/training/state.py
@@ -15,6 +15,7 @@ import time
 from modules.muse.acquisition.store import Recorder, atomic, encoded, manifest, replay
 from .sources import Sources
 from .storage import RawCache
+from .diagnostics import Diagnostics
 
 VERSION = "muse-training-1"
 CLASSES = {"double", "single", "triple", "natural", "artifact", "keypress_only"}
@@ -76,6 +77,7 @@ class Workspace:
         self.model_revision = 0
         self.records = {}
         self.raw_cache = RawCache()
+        self.diagnostics = Diagnostics(self)
         for path in sorted(self.root.glob("session-*")):
             if len(self.records) >= 64:
                 raise ValueError("session_capacity_exceeded")
@@ -119,6 +121,7 @@ class Workspace:
                 "measured_rate_hz": self.measured_rate_hz,
                 "sample_count": self.sample_count,
                 "last_device_s": self.last_device_s,
+                "sample_age_s": None if self.last_host_s is None else max(0, time.monotonic() - self.last_host_s),
                 "device_epoch": None if not self.metadata else self.metadata["clock_epoch"],
                 "quality": self.quality,
                 "reason": self.reason,
@@ -810,4 +813,7 @@ class Workspace:
         with self.lock:
             self.closed = True
             self.model_revision += 1
-        self.disconnect()
+        try:
+            self.diagnostics.close()
+        finally:
+            self.disconnect()
diff --git a/tools/muse_training.py b/tools/muse_training.py
index af0243e..67e941b 100644
--- a/tools/muse_training.py
+++ b/tools/muse_training.py
@@ -1,4 +1,5 @@
 """Build and open the local blink-training workspace with bounded process ownership."""
+
 from __future__ import annotations
 
 import argparse
@@ -6,6 +7,7 @@ import json
 import os
 from pathlib import Path
 import secrets
+import shutil
 import signal
 import subprocess
 import sys
@@ -17,60 +19,127 @@ import webbrowser
 
 def main():
     parser = argparse.ArgumentParser(description=__doc__)
-    parser.add_argument('--seconds', type=int, default=3600)
-    parser.add_argument('--no-open', action='store_true')
-    parser.add_argument('--no-build', action='store_true')
+    parser.add_argument("--seconds", type=int, default=3600)
+    parser.add_argument("--no-open", action="store_true")
+    parser.add_argument("--no-build", action="store_true")
     args = parser.parse_args()
     if not 10 <= args.seconds <= 3600:
-        parser.error('--seconds must be between 10 and 3600')
+        parser.error("--seconds must be between 10 and 3600")
     root = Path(__file__).resolve().parents[1]
+    helper = root / "artifacts/muse-bluetooth-probe"
+    swift = root / "tools/muse_bluetooth.swift"
+    plist = root / "tools/muse_bluetooth_info.plist"
+    if sys.platform == "darwin" and shutil.which("swiftc"):
+        if not helper.exists() or helper.stat().st_mtime < max(swift.stat().st_mtime, plist.stat().st_mtime):
+            cache = root / "artifacts/muse-swift-cache"
+            cache.mkdir(parents=True, exist_ok=True)
+            try:
+                subprocess.run(
+                    [
+                        "swiftc",
+                        str(swift),
+                        "-o",
+                        str(helper),
+                        "-module-cache-path",
+                        str(cache),
+                        "-Xlinker",
+                        "-sectcreate",
+                        "-Xlinker",
+                        "__TEXT",
+                        "-Xlinker",
+                        "__info_plist",
+                        "-Xlinker",
+                        str(plist),
+                    ],
+                    check=True,
+                    timeout=90,
+                )
+            except (OSError, subprocess.SubprocessError):
+                print(
+                    "Bluetooth metadata helper unavailable; the page will show that diagnostic limitation.", flush=True
+                )
     if not args.no_build:
-        subprocess.run([str(root / 'node_modules/.bin/vite'), 'build', 'apps/training',
-                        '--base', './', '--outDir', '../../artifacts/training-dist', '--emptyOutDir'],
-                       cwd=root, check=True,
-                       env={**os.environ, 'VITE_STUDIO_URL': 'https://scenescore-muse-vertical.vercel.app'})
-    if not (root / 'artifacts/training-dist/index.html').is_file():
-        parser.error('Training page is not built; omit --no-build')
+        subprocess.run(
+            [
+                str(root / "node_modules/.bin/vite"),
+                "build",
+                "apps/training",
+                "--base",
+                "./",
+                "--outDir",
+                "../../artifacts/training-dist",
+                "--emptyOutDir",
+            ],
+            cwd=root,
+            check=True,
+            env={**os.environ, "VITE_STUDIO_URL": "https://scenescore-muse-vertical.vercel.app"},
+        )
+    if not (root / "artifacts/training-dist/index.html").is_file():
+        parser.error("Training page is not built; omit --no-build")
     token = secrets.token_urlsafe(32)
-    environment = {**os.environ, 'SCENESCORE_TRAINING_TOKEN': token,
-                   'PYTHONPATH': os.pathsep.join(map(str, [root / 'artifacts/muse-training-runtime', root, root / 'src']))}
-    command = [sys.executable, '-m', 'services.training', '--port', '8767',
-               '--seconds', str(args.seconds), '--data-root', str(root / 'private_data/02A/training-web'),
-               '--web-root', str(root / 'artifacts/training-dist'),
-               '--origin', 'http://127.0.0.1:8767',
-               '--origin', 'https://scenescore-muse-vertical.vercel.app']
+    environment = {
+        **os.environ,
+        "SCENESCORE_TRAINING_TOKEN": token,
+        "PYTHONPATH": os.pathsep.join(map(str, [root / "artifacts/muse-training-runtime", root, root / "src"])),
+    }
+    command = [
+        sys.executable,
+        "-m",
+        "services.training",
+        "--port",
+        "8767",
+        "--seconds",
+        str(args.seconds),
+        "--data-root",
+        str(root / "private_data/02A/training-web"),
+        "--web-root",
+        str(root / "artifacts/training-dist"),
+        "--origin",
+        "http://127.0.0.1:8767",
+        "--origin",
+        "https://scenescore-muse-vertical.vercel.app",
+    ]
     process = subprocess.Popen(command, cwd=root, env=environment, start_new_session=True)
-    print(f'TASK JOB PID={process.pid} PGID={process.pid} PORT=8767', flush=True)
+    print(f"TASK JOB PID={process.pid} PGID={process.pid} PORT=8767", flush=True)
     previous_handler = signal.getsignal(signal.SIGTERM)
+
     def terminate(_signal, _frame):
         raise KeyboardInterrupt
+
     signal.signal(signal.SIGTERM, terminate)
     try:
         deadline = time.monotonic() + 12
         while time.monotonic() < deadline:
             if process.poll() is not None:
-                raise RuntimeError(f'Training service exited {process.returncode}')
-            request = Request('http://127.0.0.1:8767/v1/session', data=b'{}', method='POST',
-                              headers={'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token,
-                                       'Origin': 'http://127.0.0.1:8767'})
+                raise RuntimeError(f"Training service exited {process.returncode}")
+            request = Request(
+                "http://127.0.0.1:8767/v1/session",
+                data=b"{}",
+                method="POST",
+                headers={
+                    "Content-Type": "application/json",
+                    "Authorization": "Bearer " + token,
+                    "Origin": "http://127.0.0.1:8767",
+                },
+            )
             try:
-                with urlopen(request, timeout=.5) as response:
+                with urlopen(request, timeout=0.5) as response:
                     ready = json.loads(response.read(65536))
-                if isinstance(ready.get('session'), str):
+                if isinstance(ready.get("session"), str):
                     break
             except (URLError, TimeoutError, ConnectionError):
-                time.sleep(.1)
+                time.sleep(0.1)
         else:
-            raise RuntimeError('Training service did not become ready')
-        url = 'http://127.0.0.1:8767/#token=' + token
-        print('Training page: http://127.0.0.1:8767 · no stream or recording starts automatically.', flush=True)
+            raise RuntimeError("Training service did not become ready")
+        url = "http://127.0.0.1:8767/#token=" + token
+        print("Training page: http://127.0.0.1:8767 · no stream or recording starts automatically.", flush=True)
         if not args.no_open:
             webbrowser.open(url)
         else:
-            print('Token is printed by the local service; paste it into the training page.', flush=True)
+            print("Token is printed by the local service; paste it into the training page.", flush=True)
         process.wait(timeout=args.seconds + 15)
         if process.returncode != 0:
-            raise RuntimeError(f'Training service exited {process.returncode}')
+            raise RuntimeError(f"Training service exited {process.returncode}")
     except KeyboardInterrupt:
         pass
     finally:
@@ -82,8 +151,8 @@ def main():
         except subprocess.TimeoutExpired:
             os.killpg(process.pid, signal.SIGKILL)
             process.wait(timeout=3)
-        print(f'STOPPED PID={process.pid} exit={process.returncode}', flush=True)
+        print(f"STOPPED PID={process.pid} exit={process.returncode}", flush=True)
 
 
-if __name__ == '__main__':
+if __name__ == "__main__":
     main()
