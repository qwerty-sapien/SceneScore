# 02C integration request — no contract or dependency amendment

Status: ready for integrator review. No shared file was edited by 02C.

Mount `modules.music.api.router` under `/music`; export `health()` as specified by the frozen service interface. It is read-only and starts no render/audio/network job. The integrator may include `modules/music/tests` in the root pytest target and `modules/music` in Ruff. Imports need the repository root as well as `src` on PYTHONPATH (`PYTHONPATH=.:src`). Runtime synthesis/MIDI use Python stdlib and the already locked canonical validator; no new dependency is requested.

The exact 0.1 catalogues come from `modules.music.catalog.get_composition(id, version=1, variation='base')` and `get_groove(id, version=1)`. `build_score` supplies a documented `SceneScoreMusicScore` v1 sidecar for lane/part details absent from CompositionSpec. The canonical notes are the complete lead; complete score events include accompaniment. Each variation shares the seed catalogue ID but has different exact composition bytes/provenance: approval/cache must bind the content hash and selected variation.

`modules.music.events.resolve_events(composition, groove, plan_id, object_id, humanize=True, variation='base')` returns canonical ScoreEvent records. Exact registered instrument/timbre IDs are `keyboard_damped_v1`, `bass_pluck_v1`, `object_bell_v1`, `brush_noise_v1`; lanes are `piano_or_lead`, `bass`, `object_motif`, `brushes`. Object markers retain the supplied stable object ID. The arranger should bind approved motif owners to actual source objects, use canonical expression lanes and signed ±2 transition policy, and preserve score event IDs or create explicitly versioned derived events. Default authoring ID `object-motif` is synthetic, not an inspected Blender identity.

`transpose_events` changes copied pitched-note MIDI numbers only, preserving dynamics/articulation/brush/Foley fields. The arranger owns modulation eligibility, prepared harmonic revoicing, approved hashes, scene-event effects, boundary scheduling and multiple-object ownership. No scene manifest was used to claim a synchronized music render. The score has explicit melodic rest windows, including the hero's answer gaps; align/approve the desired pre-impact space against actual scene events in integration. Do not label the offline audition exports synchronized to a rendered animation.

`render_arrangement(id, output_dir, variation='base', sample_rate=48000, *, output_root=None, budget=None, cancellation=None)` is an offline authoring API, returning a module render manifest with canonical assets and a saved RunManifest. It creates a new scoped directory only. Resource/path/cache/cancellation tests exist; the lock is process-local. Any future executable service capability must supply a scoped output path and budget and impose cross-process concurrency through the integrator harness. Do not register the read-only API as evidence that a production audio callback exists. The user authorized 2A–2D only; browser execution and audiovisual integration remain later work.

Generated artifacts are ignored by Git and must be copied from the worker's `artifacts/music/review-v2/` before deleting its worktree. The preserved tracked audit at `modules/music/fixtures/validation-48k.json` identifies actual WAV/manifest hashes. The root can recheck after copying with `PYTHONPATH=.:src .venv/bin/python -m modules.music.audit --root artifacts/music/review-v2`.

All files contain original project drafts and procedural synthesis; no third-party melody/sample downloads. General distribution terms for these outputs have not been selected. Human audition, real instruments, browser audio, physical playback timing and prepared modulation listening remain unverified/NOT_RUN. This does not block the offline authoring path.
