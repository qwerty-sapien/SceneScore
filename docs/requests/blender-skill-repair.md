# Next-task brief: reference-guided Blender authoring repair

Use the user's current animation-skill request, root AGENTS, VISION and the new
`physics-aware-animation` skill. This brief proposes the next bounded work; it is
not evidence that the renderer or any production candidate has been repaired.
Keep Phase 4, Muse changes, approval bypasses, frozen-threshold edits and immutable
packs out of scope. Confirm the new run's compute/time budget before heavy jobs.

## Work in order

**A. Restore reproducible execution.** Identify missing recorded assets; restore
or rebuild necessary control candidates into NEW directories. Resolve verified
Blender/FFmpeg/MCP paths and recreate the pinned project environment as necessary.
Apply/test the optional MCP result-handling patch; investigate the separate
upstream addon-status problem without silently updating vendor code. A status
error may coexist with working inspection, but must remain visible/nonzero.

**B. Build one genuinely stronger hero inside a supported scope.** Use the two
supplied recordings for layout/staging/material references, not as certified
physics. Start with one sphere and fixed obstacles to exploit existing mechanics.
Author an original 12–20 s route with an acceleration phase, an oblique rebound,
and a later positive-gap near miss. Keep the ending visible and supported. This
is a target brief, not a solved trajectory. Adjust physical geometry/initial
conditions within declared bounds to obtain the event order and separation; do
not force it with outcome keyframes. A fixed post is suitable for this first
backend; a moving gate or colliding second ball is not.

Introduce a versioned scene-intent supplement, a small reusable component API,
and an explicit plan-to-builder adapter. The included example JSON is not already
wired into the CLI. The current trusted-ID check must be extended through reviewed
registry changes for a genuinely new scene; do not modify immutable old seeds.
Preserve current playback/export/handoff/selection identities and tests.

**C. Close the production loop.** First graybox motion; then a well-composed full
preview; then final lighting/material/render work. Use MCP for inspection and
small scratch iterations, but capture all accepted changes in source. Run existing
physical/replay/media gates plus measured story/event spacing, camera visibility
and full native-motion review. Do not judge only a random still sample. Final
beauty promotion requires both valid mechanics and visual/story evidence.

**D. Repair broader physics separately.** Reproduce the failed native controls on
the verified runtime; use the existing restitution diagnosis as a hypothesis to
check, not a universal fact about Blender. Establish a bounded new experiment and
unchanged acceptance criteria. Close the actual stale-cache, fabricated-miss and
release-impulse negative-control gaps recorded in the addendum. No extra hero
complexity until applicable backend controls pass.

**E. Prove generalization and preserve the music product.** Once the first hero
works, retain it as an executable exemplar and test held-out layouts, radii,
release conditions and cameras. Expand next to a supported two-body interaction
and a constrained launcher. Do not claim a versatile skill from one scene or
three camera views of the same mechanism. Keep identities, raw contacts, selected
salient events, exact Foley timing and editable music. Current excerpts remain
excerpts unless adaptive scoring is separately implemented and auditioned.

## Required output from the coding agent

Return a scoped source diff, commands and actual results, new candidate paths,
`.blend`/trace/preview, measured event evidence, unchanged threshold provenance,
full-motion review status, before/after visual comparison, mutation outcomes and
remaining limitations. Clearly separate numerical pass, visual acceptance and
human approval. Missing tools/evidence must be reported, not replaced with prose
claiming the next stage succeeded.
