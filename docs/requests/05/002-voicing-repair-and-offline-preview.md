# Node 05 repair iteration 1

Independent review found three real defects in the initial prepared API: omitted defining thirds in 40 default slots, 64/72 acknowledgement dyads not matching the declared guide tones, and an avoidable octave drop when tonic-indexed variants wrapped from 4 to 6.

The integrator repaired these in module `music-vertical-transitions-2`. The supplied table bytes remain unchanged. Concrete preparation requires defining chord tones before minimizing movement: third/seventh for dominant/minor seventh, third/flat fifth/seventh for altered dominant, and third/sixth for the table's tonic 6/9 pitch sets. All 216 entries still admit the original three-semitone voice-leading bound. Acknowledgements select the first chord's third/seventh explicitly and stay at velocity <=60.

`PreparedVerticalTransitions.variants` now maps original event ID to a MIDI-pitch-indexed map. `verticalPitchVariant(prepared, currentEvent, signedStep)` applies exactly +2 or -2 to the current event, folding only at the approved register bound. Pass the current prepared variant on subsequent requests; do not derive pitch from tonic class or total displacement modulo 12. `variant_origins` resolves cached variants. At most 512 harmonic source events are prepared; all pitch alternatives stay inside the plan register. `verticalEventFragments` similarly accepts the current event. Old tonic-indexed calls are invalid in v2. The legacy `transition()` API is unchanged.

`packages/audio/vertical-preview.ts` is an offline TransitionPreview helper for deterministic draft audition data while shared engine integration awaits coordination. It derives signed direction from the plan's causal scene policy, avoids overlapping near-miss holds, preserves unaffected lanes, and returns `approval:null`. Its +20ms acknowledgement is a **planned scene timestamp**, not a measured scheduled or acoustic onset. It never constructs or accepts a ControlAction, calls an approval API, or exposes a live control shortcut. Whole-score materialization is offline only.

The root's `tools/render_music_vertical.ts` reuses the existing `voice()` renderer and emits a static stereo PCM mix and four stems. It does not run an AudioContext or infer browser/physical latency. Its output manifest states this limitation. Actual Timeline/Engine scheduling, human approval, live browser and physical output gates remain separate.

Initial repair checks: 10 Python transition tests and 12 TypeScript transition/preview tests passed; root typecheck passed. A defining-tone assertion was then added to Python to preserve the repaired requirement. Final combined evidence and independent re-review belong in the execution ledger; previous handoff's voicing digest is historical and superseded by this iteration.

Final repair verification: 10 Python transition tests and 14 TypeScript transition/preview/envelope tests passed; typecheck passed (session 94620, exit 0). Independent review closes all five findings. Actual cross-language 216-entry pitch digest is `0ee37ce2a93f1a0752e64993abb0e8502f1fab733ebade804b0685080a59c284`; exact serialization and reviewed file hashes are in `reports/music-vertical/TRANSITION-REVIEW.json`. This does not close runtime node 06 or human audition.
