# Phase 02D — 02D

Immutable task source: [prompt](../../.brief/prompts/02D_arranger.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 1 frozen contracts and fixtures.

Inputs: Scene/music fixtures; optional verified configured API.

Owned paths: modules/arranger/; module-local tests; artifacts/arranger/. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: Fixture-to-baseline approved-capable plan, manual import/export, constrained modulation and golden ScoreEvents; API status honestly tested/not-run; audition handoff.

Handoff path: `handoffs/02D.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/02D/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
