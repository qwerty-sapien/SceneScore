# Phase 03B — integrator

Immutable task source: [prompt](../../.brief/prompts/03B_integration.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 2B–2E handoffs and Phase 2A baseline or labelled replay/keyboard.

Inputs: All worker handoffs and approved assets; trained detector optional.

Owned paths: services/local/; contracts/; root scripts/locks; tests/; docs/STATUS.md; cross-module integration fixes with recorded ownership transfer. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: One-command exercised replay demo; one exported approved performance; default controls/clock/provenance correct; live path only claimed if exercised; fallback works.

Handoff path: `handoffs/03B.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/03B/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
