# Phase 02C — 02C

Immutable task source: [prompt](../../.brief/prompts/02C_music_assets.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 1 frozen contracts.

Inputs: Composition and brush seeds; audio/event fixtures.

Owned paths: modules/music/; module-local tests; artifacts/music/. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: One complete audible hero arrangement; four symbolic forms; three brush recipes; golden render/event fixtures; exact catalogue lookup; explicit human AUDITION_PENDING where not listened.

Handoff path: `handoffs/02C.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/02C/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
