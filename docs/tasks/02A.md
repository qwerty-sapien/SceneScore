# Phase 02A — 02A

Immutable task source: [prompt](../../.brief/prompts/02A_muse_acquisition.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 1 frozen contracts.

Inputs: Device access when supplied; RAGTM snapshot; acquisition fixtures.

Owned paths: modules/muse/acquisition/; modules/muse/baseline/; modules/muse/annotation/; module-local tests; private_data/02A/. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: Recorder/annotation/replay and fault tests; unchanged baseline adapter evidence; genuine recording if provided, otherwise HARDWARE_UNVERIFIED at recorder/fixture boundary.

Handoff path: `handoffs/02A.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/02A/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
