# Phase 02B — 02B

Immutable task source: [prompt](../../.brief/prompts/02B_blender_assets.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 1 frozen contracts.

Inputs: Recipe seeds, scene contracts; configured Blender when available.

Owned paths: modules/blender/; module-local tests; artifacts/blender/. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: Verified hero bundle and validated script/export path for all ten recipes; geometry tests, summary fixtures, actual render and visual QA statuses. Missing Blender blocks rendering evidence only.

Handoff path: `handoffs/02B.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/02B/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
