# Phase 03A — 03A

Immutable task source: [prompt](../../.brief/prompts/03A_muse_training.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 2A plus genuine independently labelled sessions.

Inputs: Locked session split, raw hashes, unchanged baseline and grammar.

Owned paths: modules/muse/training/; modules/muse/evaluation/; modules/muse/detectors/; their local tests; models/local/03A/; reports/03A/. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: Held-out real evaluation and fault/replay tests; versioned safe deployment artifact, calibration/rollback commands, evaluation card and failure taxonomy. Missing genuine data blocks training, never invent results.

Handoff path: `handoffs/03A.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/03A/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
