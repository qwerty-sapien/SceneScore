# Phase 04 — release

Immutable task source: [prompt](../../.brief/prompts/04_release_rehearsal.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 3B integration gate.

Inputs: All handoffs, requirements evidence, rules decision and audition records.

Owned paths: reports/release/; docs/RUNBOOK.md; release evidence; final cross-module fixes coordinated with integrator. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: Independent MUST audit, resource cleanup, honest release status, five-hour rehearsal record and scoped demo packaging; no new features.

Handoff path: `handoffs/04.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/04/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
