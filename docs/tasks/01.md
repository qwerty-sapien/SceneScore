# Phase 01 — integrator

Immutable task source: [prompt](../../.brief/prompts/01_contract_harness.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 0 documentation gate.

Inputs: AGENTS, VISION, CONTRACTS, EVALUATION, all seeds.

Owned paths: contracts/; fixtures/; services/local/; root setup/locks/scripts/CI; tools/; tests/; runtime skeletons only. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: Clean install/doctor and real passing shared fixture tests; freeze 0.1 and Git worktree base; publish router interfaces. No domain implementation.

Handoff path: `handoffs/01.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/01/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
