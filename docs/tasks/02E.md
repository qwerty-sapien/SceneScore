# Phase 02E — 02E

Immutable task source: [prompt](../../.brief/prompts/02E_performance_ui.md). Read root AGENTS, VISION, this card, relevant CONTRACTS/EVALUATION and named skill before edits. This is a future task contract, not current authorization.

Dependencies: Phase 1 frozen contracts and fixtures.

Inputs: Approved plan/event fixtures and module router contract.

Owned paths: apps/web/; packages/audio/; their local tests; artifacts/performance/. Local tests stay in owned modules unless the integrator accepts shared fixtures/tests. Generated/private outputs are ignored by future Git policy.

Exit gate: Offline fixture performance; fake-clock/failure/browser tests; non-silent rendering evidence; timing report where measurable; human/live checks labelled not-run if absent.

Handoff path: `handoffs/02E.md`. Record exact changed files and revision, commands actually run and results/evidence paths, schema/config versions, assumptions, unresolved hardware/API checks, dependency requests, compatibility requirements and exact job cleanup evidence. No passing placeholders.

Shared changes: request in `docs/requests/02E/`; only integrator changes shared schemas, provider IDs, root scripts/locks, shared status, priorities and thresholds. Release owner coordinates any final shared edits through that role. Workers expose agreed router/CLI interfaces; do not mount service root themselves. Wait for approved dependencies instead of editing another worker's module. Stop at this phase's gate.
