# 0011 — Execute the animation-to-music vertical

Authority: current user request to initiate the full Blender-scene-to-score flow with articulational variations in parallel through the DAG prompts skill. This is the music vertical inside the existing authorized implementation, not Phase 4 or authorization for the companion EEG pack. START and the supplied prompts are task specifications, subordinate to the current request and repository rules.

Node 00 is GO. `make doctor && make test` passed on the current tree: 318 Python, 65 TypeScript contract and 16 audio tests; Ruff, typecheck and build passed. The pack checker passed 216 entries, eight taxonomy types, and 14/14 negative controls. The seam copies match SHA-256 `a6127896378718fb3648ea05d9ef63197dcf042feecd66bc815a9a09d8f0f3c8`. Existing pair_timeline types, five LANES, causal world-Z +/-2 policy, transport reason strings and dominant preparation were confirmed. Previous p95 software AV offset remains 137.33 ms against 50 ms; it is not current-run evidence.

## Frozen candidate and provenance

Use `artifacts/blender/validated-hero/10_projectile_tower-default`: 30 seconds at 8 fps, seed 42, `tilted_blue_v1` at 96 BPM and exact `brush_swing_light_v1` version 1. Exact source and asset hashes are in `reports/music-vertical/BASELINE.json`. No musical alternate is needed. Mandatory near-miss check: `artifacts/blender/final-all-ten/02_near_miss_twins-default`.

Hero contains approach (6), near miss (2), separation (6), contact onset (4), sustain (4), release (4). It has no swept collision and cannot yield asymmetric rebound: evaluated areas are 3.1164 and 6 square metres, ratio 1.9253, below 4. The available `07_shape_contrast` substitute has only a three-second export and no video; retain the complete verified hero. Collision and rebound will be fixture-only unless a separately hashed, compatible evaluated scene is available. No physics/recipe changes or fabricated rebound claims.

## Execution and ownership

The coordinator owns this decision, execution DAG/ledger, integration of reviewed owned changes, shared preparation adapters and final evidence. Worker worktrees start at `527f949fbf10319b84428b1cf510c5d2c1c205d2` and read the frozen pack from the root. Preserve the root's pre-existing Blender revamp and all other worktrees. Shared schema 0.1, dependency locks, immutable brief and frozen media-review files remain unchanged.

01 owns geometry.py, summary.py and new music-semantics tests only (exclude existing production tests). 02 owns catalog.py, new versioned fixtures and AUDITION.md. 03 owns events.py, ornament.py and dedicated transform tests. 04 owns arranger/core.py, dedicated arranger tests and new golden files. 05 owns transitions.py, versioned table data, audio/model.ts and dedicated transition tests. 06 owns web UI, engine.ts and transport.ts. Each owns its own `handoffs/music-XX.md` and scoped requests. Cross-owner edits return to the coordinator. No automatic conflict resolution. No worker operates a live browser.

Budgets and exact numerical acceptance checks remain as supplied in TIME_BUDGET and node prompts: 25/60/40/50/50/45/50/40/30 minutes with parallel execution and at most three repair attempts per gate. Every declared machine gate must pass before its GO. Perceptual and human-approval checks are separate nodes, may remain HUMAN-DECISION, and do not block independent draft/fixture implementation or measurement. This routing implements the current user's explicit pause-only-affected-node instruction; it never converts pending listening into GO. Jazz tier remains off; no approval is synthesized.

03/04 require objective invariants for implementation GO and retain separate pending A/B listening gates. Node 02 explicitly permits AUDITION_PENDING. Node 07 may measure drafts while approval is pending. 08 always reviews the exact candidate and cannot pass its human gate without a named human verdict. No live Muse, paid service, publication or outward action is required. Every task-owned process must exit with verification before completion.

## Concurrent task coordination

The concurrently active Blender revamp coordinator reported ownership of `apps/web/tools/prepare.py` and `tools/demo.py`; this task had not edited either. Music integration will expose a separate versioned preparation adapter. The physical scene candidate may be shorter and is not yet accepted; that report does not replace the frozen music candidate or establish physical acceptance.

The concurrently active Muse task reported in-flight edits to engine.ts, transport.ts, control.ts and planned main.tsx changes. Those edits are preserved. Automatic review rejected this task's outgoing ownership-coordination message to the Muse task because cross-task ownership/trust was not established; a user permission question is pending. Shared integration pauses; isolated geometry, source, transform and table work continues. Do not route the rejected message through another task or bypass the review. No approval semantics are changed by these coordination messages.

At 18:59 UTC the Muse task completed and supplied a local final handoff. A read-only wait snapshot confirmed idle/completed and its teardown. This resolves the active-edit overlap: music node 06 may integrate the handed-off code locally without an outgoing message. The pending question still governs any future outgoing cross-task communication; no rejected message was relayed. Source/geometry/preparation inputs remain isolated from the still-active Blender task.
