# Prepare animation directions 06, 08 and 18

Authority: the user's request to prepare these three animations using the framework/method of `artifacts/blender/revamp/analytic/05_bouncing_staircase-32/renders/diagnostic/beauty/video.mp4`.

The authorized output is three new thirty-second animation studies, editable Blender scenes, computed motion and verification evidence. Preserve the referenced staircase, earlier candidates, frozen schemas and unrelated training work. No website deployment, Phase 4 release or human musical approval is implied.

Reuse the successful method: explicit SI mechanics, fixed-rate integration, saved motion at 240 Hz, dependency-graph extraction, fresh-process verification of a 30 fps replay, then bounded Blender rendering with full frame/decoding checks. Do not replace physically computed trajectories with authored pose curves or retime motion to fit a script.

The old fixed-OBB sphere backend does not cover every selected actor. New scoped models must state their constraints: guided rolling with actual rotational inertia for 06, a polished sliding bead in an ideal visible groove for 08, and a supported planar puck with coupled hinged-fin response for 18. Direction 08 uses smoothly connected descending grooves between its counterwound levels in place of the proposed free drop and bowl drain. These are disclosed implementation adaptations; no unsupported three-dimensional rolling or impact claim is made. Physical guides, rail surfaces, pivots and power sources must be visible and match those assumptions. Check independently known motion, energy/impulse behavior, convergence, geometry/support, scale and fresh replay. Preserve any failed trials.

The direction scripts are creative intent; timings and routine set geometry may be tuned through new simulations. Disclose adaptations. The sphere/cylinder/hoop comparison, multilevel spiral journey, and greenhouse contact/near-miss/hinged-fin chain are the central behaviors to retain.

Heavy Blender and encoder jobs run serially, with two CPU threads, bounded wall time, recorded process groups and verified cleanup. Workers use separate worktrees and only new owned modules/tests; the integrator owns the runner, scene builder, reports and rendering. Every derivative gets new paths and provenance.
