# Phase 2A handoff — recorder, replay, annotations and causal baselines

Gate: **PIPELINE_TESTED_ONLY / HARDWARE_UNVERIFIED**. The complete owned recorder/test-fixture boundary is implemented. No worn-headset capture, participant session, training, empirical accuracy evaluation, camera/audio capture, game import or cloud request occurred. Muse remains optional; keyboard/replay product tracks are unaffected. Phase 3A has not started.

## Delivered boundary

- `modules/muse/acquisition/store.py`: versioned local raw manifest, atomic bounded chunks, unchanged numeric samples and per-sample clocks, continuity validation, clean close, recover, local export and confirmed deletion. Existing session directories cannot be overwritten. No interpolation/filtering of raw data.
- `modules/muse/acquisition/live.py`: one optional explicit existing-source LSL receiver, strict operator-attested metadata/source/units/channel checks, 600-second maximum foreground capture, cancellation/disconnect cleanup. Actual pylsl is **not installed** in the inspected project Python; no dependency installed. The adapter is tested with fake inlets only. LSL source times and host batch receipt are distinct; packet-loss counts are rate-based estimates; contact quality is unverified, so the live route never arms controls automatically. Reconnect is an explicit new session/fresh epoch plus warmup/rearm.
- `modules/muse/acquisition/__main__.py`: health, file recording, capture-lsl, replay/detect, inspect, recovery, export, delete, intent cues, delayed confirmations and append-only independent review/relabel.
- `modules/muse/acquisition/api.py`: agreed synchronous `health()` and read-only APIRouter (GET `/health`, POST `/diagnostics`). Mounting belongs to integrator. No arbitrary file-reading HTTP endpoint or HTTP-triggered recording.
- `modules/muse/annotation/`: versioned separate cue/response/label JSONL, actual source-epoch timing bounds and independent evidence/reviewer requirements. Cue/prediction sources cannot become truth; synthetic sessions cannot become real ground truth. Label revision history is retained.
- `modules/muse/baseline/ragtm.py`, license/lineage and exact-source oracle: preserved original frontend candidate default/order semantics behind a named Python port. This is **not** claimed to preserve the full backend/front-end pipeline. Source/backend dependencies and clock/upload differences are documented; original repository is read-only.
- `modules/muse/baseline/causal.py`: separately named NEW deterministic causal low-pass/median-MAD candidate path, bilateral agreement, hysteresis and duration limits, plus explicit arming/fault reset and closed-sequence grammar. Default singles/triples/ambiguous trains suppress actions. Outputs are canonical uncalibrated candidates/gestures, **never direct musical actions**. Triple/amplitude expression remains disabled.
- Module READMEs, local tests and `docs/requests/02A/integration.md` document limits and shared integration requests. No shared schema, lock, provider ID, root source or service-mount file was edited.

## Actual validation and evidence

Working directory: `/private/tmp/scenescore-wave2-04j2z7nl/02A`, branch `wave2/02A`, base `contracts-v0.1`. Existing project Python: `/Users/agent/Desktop/SceneScore/.venv/bin/python`. No broad dependency installation performed.

Executed commands:

```sh
PYTHONPATH=.:src /Users/agent/Desktop/SceneScore/.venv/bin/python -m pytest modules/muse -q
/Users/agent/Desktop/SceneScore/.venv/bin/python -m ruff check modules/muse/acquisition modules/muse/baseline modules/muse/annotation
PYTHONPATH=.:src /Users/agent/Desktop/SceneScore/.venv/bin/python -m modules.muse.acquisition health
PYTHONPATH=.:src /Users/agent/Desktop/SceneScore/.venv/bin/python -m modules.muse.acquisition --help
git diff --check
```

The final module suite passed **50 tests**. Tests include actual CLI main invocations for file import/replay/export/delete, exact raw round trip, gaps/reconnect, atomic crash recovery, input corruption, symlink/exclusive-path checks, consent/metadata guards, fake LSL cancellation/disconnect cleanup and unit mismatch, read-only API, independent label/cue separation, candidate partition invariance, grammar closure/triple rejection, quality loss/rearm and stale/malformed input. RAGTM port decisions match the verbatim extracted frontend candidate statements on **1,500 seeded synthetic filtered samples**. The bounded Node oracle is a software comparator, not an unchanged real recording benchmark. Two existing upstream FastAPI/Starlette TestClient deprecation warnings remain visible.

Evidence: `modules/muse/acquisition/evidence/{pytest.txt,ruff.txt,health.json,cli-help.txt,checks.json}` and `modules/muse/baseline/lineage.json`. Source files were hashed against the supplied actual RAGTM paths. The initial test pass exposed a test's inclusive-closure-boundary expectation (fixed test timing); a later fake transport test exposed an unsupported `unknown` quality string (changed to canonical `unverified`). Four unused test imports were removed by scoped ruff. Final tests/lint were rerun after fixes. No error is hidden as a pass.

Not run: real Bluetooth/LSL connection, actual device identity/rate/unit measurement, participant consent/session collection, genuine held-out replay, original complete RAGTM filtering/runtime equivalence, live latency/physical timing, statistical blink accuracy, trained model comparison or audio integration. No listening or readiness claim follows from these tests.

## Integration and remaining human work

The integrator should mount the read-only router and include module-local tests. Keep optional-pylsl status and hardware uncertainty visible. Metadata/labels/raw sessions stay under ignored `private_data/02A/`; no private raw deliverable was produced. The long-train rejection diagnostic is module-owned because frozen GestureEvent count currently permits only 1–3; see the scoped request for a possible future shared amendment. Do not mislabel the RAGTM candidate port as the entire original detector.

A human/operator must establish the actual headset/transport, install a compatible optional LSL dependency through integrator governance, independently verify units/rate/order, explicitly start a visible recording, and supply consented independent annotations/refit sessions. Contact-quality verification and clock correspondence remain unresolved. No arbitrary universal threshold is calibrated; current Config defaults are exploratory and output-hashed. Labels/splits/event matcher must be independently audited and frozen before empirical model comparison.

Exact next phase instruction (requires a separate user authorization):

> Read `.brief/prompts/03A_muse_training.md`, `AGENTS.md`, `docs/VISION.md`, `docs/EVALUATION.md`, `docs/LINEAGE.md`, `docs/CONTRACTS.md`, `.agents/skills/blink-evaluation/SKILL.md` and `handoffs/02A.md`. Execute Phase 3A only in its owned worktree. Audit real independently labelled refit sessions and freeze grouped splits before any feature fitting. Preserve both the named RAGTM candidate-port comparator and the NEW causal baseline; do not claim complete RAGTM equivalence. If genuine sessions or required access are absent, implement only the training/evaluation software boundary and report `INSUFFICIENT_REAL_DATA`; never present synthetic results as Muse performance. Stop at the Phase 3A gate.

## Resources and revision

No live stream, persistent server, background worker, daemon, container or detached shell was started. Bounded command tool sessions `89227`, `48741`, `61207`, `61111` and `68128` all returned terminal exit results. TestClient contexts exited, fake inlets were verified closed, and bounded Node oracle subprocesses completed within their ten-second timeout. No private session or export requires preservation outside committed module tests/evidence. The worker commit contains only owned modules, scoped request and this handoff; the integrator should preserve all tracked evidence when removing the worktree.
