# Phase 3B handoff

The integrator accepted post-2E ownership of web/audio changes and root service/scripts/tests in decision `0003-integrated-studio.md`. No schema/dependency version changed. Read `reports/phase3/GATE.md` before future work; a green software suite is not the formal product exit gate.

Run `make demo` from the greenfield root with the existing locked dependencies and preserved `artifacts/blender/` bundles. It prepares six local scene/groove combinations, builds and serves 127.0.0.1:8765. Open that URL, audition the draft, inspect its exact plan, and let a human approve before conducting. Optional Muse/API remain unavailable. The initial synthetic replay requests once at 18 seconds only while approved and armed; choose a moving world-Z focus such as tower-2. The default projectile's stationary world-Z correctly holds the key.

Export buttons download a mix, four stems and an editable score/control/provenance JSON. Approved export requires a complete uninterrupted run; seeking invalidates it. Draft exports retain `approval:null`. To mux the actually exported draft:

```sh
PYTHONPATH=.:src .venv/bin/python tools/mux_performance.py artifacts/phase3/performance/scenescore-DRAFT.json artifacts/blender/validated-hero/10_projectile_tower-default/preview.mp4 --out artifacts/phase3/performance/NEW-DRAFT.mp4
```

The mux command refuses overwrite, stale hashes, wrong duration/offset and unaligned stems. It does not create approval. No external model executes in audio playback. Mixer automation is recorded in scene time; offline rendering resolves the same symbolic performance, not a recording of the physical output. Pause wall time is omitted from project-time export. Actual physical latency and human listening still require evidence.

Checks actually run and measured outcomes are in the gate and `checks.log`. The real approved path was blocked by automatic approval review; its unresolved human request must not be bypassed. No trained Muse model or listening decision was invented. No Phase 4 work ran.

Task-owned resources: Phase 2E Vite PID 45697/session 8798/port 5176 was terminated when integration began. Initial sandboxed launcher PID 21474 exited after loopback binding was denied. Retried loopback demo PID 33970/session 6357/port 8765 is task-owned and must be stopped before final response. Final verified teardown is appended below.

Final teardown: PID 33970 received TERM and Uvicorn reported completed shutdown; its session exited 143. Vite session 8798 also exited 143. `reports/phase3/teardown.json` records all three PIDs absent and no listener on either task port. Playwright page closed. No container, capture, real training or new Blender job started. Completed 2E/3A worktrees were removed after verifying all nine prepared public files preserved identically in root; source commits 7efee98/759bc48 remain in Git and were integrated as 061719a/c62d847. Evaluator owns only bounded analysis work and must report its own cleanup before final integration.
