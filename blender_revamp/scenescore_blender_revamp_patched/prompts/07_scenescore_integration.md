# 07: connect the new animation to the existing product

Depends on 06. Coordinate all shared-schema/root-script changes. Limit scope to changes caused by the animation revamp.

1. Export from the exact finalized motion used for rendering. Preserve documented units, handedness, quaternion order, frame origin and half-open render interval, or migrate explicitly. Derive object states and interactions from evaluated geometry, with reliable identities and uncertainty/method labels. Replace fixed-axis assumptions where objects now rotate. Never infer a collision impulse from a distance alone or mark an event physically measured without appropriate evidence. Keep impulse unknown when unavailable.
2. Separate physical participation from musical ownership. Supports may collide without owning a voice; decorative geometry must not create unintended voices or event floods. Evaluate which actor parts need physical/event participation. Preserve persistent sonic identities and score mapping where semantics remain stable.
3. Trace every active asset consumer, including browser preparation, copied public/build assets, API summaries, audition preparation, sample-selection tools and final mux. The snapshot hardcodes old hero/control folders in preparation. Replace stale active selection with an explicit verified candidate selection using the existing repo conventions. Do not silently mix new video with old transforms, interactions or score schedules.
4. Rebuild dependent event/Foley schedules for the new timing and preserve enough existing music compatibility to keep the product runnable. Do not redesign the soundtrack in this animation milestone. Export a clean, authoritative scene clock plus robust time-indexed motion/event features needed by the separate adaptive-music milestone, using only quantities genuinely available from evaluated scene data (for example speed/acceleration/angular motion and release/contact/settling events where defined). Separate exact collision/Foley times from musical quantization policy. Matched misses produce no tower-impact Foley, but may legitimately produce later floor contacts. A changed scene/plan invalidates earlier approval; export remains a clearly labelled draft until real approval exists.
5. Verify build, schema compatibility/migrations, API behavior, browser playback, source/video identities and mux decoding. Inspect synchronization at release/impact/settling markers. Higher video cadence alone does not prove the existing timing problem is fixed; rerun the relevant software measurement and distinguish it from physical display/acoustic latency.
6. Preserve frozen historical reviews and their source assets. Write a new report and lineage, update current documentation/status honestly, and remove obsolete instructions from active guidance without rewriting immutable inputs. Keep keyboard/replay and optional Muse behavior working. Do not train models, implement blink-to-harmony logic or redesign the audio engine in this scope; expose stable outputs for those separate DAGs instead.

Deliver an integrated runnable candidate, updated local generation/review skill guidance, focused regression results, exact reproduction commands and an approval-safe export. Root `make test` or its current equivalent must be run where feasible, with failures and blocked tests distinguished. The animation milestone does not silently approve the whole product.
